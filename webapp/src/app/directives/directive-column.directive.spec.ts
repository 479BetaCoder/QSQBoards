import { DirectiveColumnDirective } from './directive-column.directive';

describe('DirectiveColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveColumnDirective();
    expect(directive).toBeTruthy();
  });
});
